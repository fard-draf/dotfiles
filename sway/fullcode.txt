Fait un script pour installer TOUTES les dependances necessaire a l execution de SWAY et des dependances !

sway/.config/sway/config:
<code>
### Variables
set $mod Mod4
set $left h
set $down j
set $up k
set $right l
set $term term  # Or use kitty/alacritty if preferred for better performance in dev workflows
set $menu /usr/bin/wofi # Consider fuzzel or rofi-wayland for more features
set $editor /usr/bin/hx  # Assuming VS Code; change to vim/nvim/emacs as needed
set $browser /usr/bin/brave-browser  # Or chromium/brave for dev tools

### Start Exec
exec mako #Notifications
exec kanshi #Screen conf

### Output configuration
# Use kanshi for dynamic multi-monitor setups (uncomment and install if needed)
# exec kanshi
# Default wallpaper
output * bg /home/fard/Pictures/Wallpapers/will-rust-L76ogaaqsAk-unsplash.jpg fill
# output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill
# Manual screen setup bind (your custom script)
bindsym $mod+Shift+x exec /home/fard/dotfiles/sway/scripts/setup-dock.sh
# Example for dual monitors (adjust based on swaymsg -t get_outputs)
# output eDP-1 pos 0 0 res 1920x1080
# output HDMI-A-1 pos 1920 0 res 1920x1080


### Idle configuration
# Enhanced idle with swayidle: lock screen, dim displays, and DPMS
exec swayidle -w \
    timeout 300 'swaylock -f -c 000000 --image /path/to/lockscreen.png' \
    timeout 600 'swaymsg "output * power off"' \
    resume 'swaymsg "output * power on"' \
    before-sleep 'swaylock -f -c 000000'

### Input configuration
# Keyboard: set layout, repeat rate for faster typing in dev
input keyboard {
    xkb_layout us
    xkb_variant intl
    repeat_delay 250
    repeat_rate 50
}

#Pave numerique
input type:keyboard xkb_numlock enabled
# Touchpad: natural scroll, tap-to-click for productivity
input "type:touchpad" {
    dwt enabled
    tap enabled
    natural_scroll enabled
    middle_emulation enabled
    scroll_factor 0.8  # Adjust sensitivity
}
# Mouse: accel for precise control
input "type:pointer" {
    accel_profile adaptive
    natural_scroll enabled
    pointer_accel 0.2
}



### Key bindings
# Basics
bindsym $mod+Return exec gtk-launch wezterm-local-zellij
bindsym F1 exec gtk-launch wezterm-remote-zellij.desktop
bindsym F2 exec wezterm
bindsym $mod+q kill
bindsym $mod+d exec /usr/bin/wofi --show drun --allow-images
floating_modifier $mod normal
bindsym $mod+Shift+c reload
bindsym $mod+Shift+e exec swaynag -t warning -m 'Exit sway?' -B 'Yes' 'swaymsg exit'

# Dev-specific: Quick open editor/browser
bindsym $mod+e exec $editor
bindsym $mod+b exec $browser

# Moving around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Workspaces (1-10, with dev-oriented names optional)
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# Layout
# bindsym $mod+b splith
bindsym $mod+v splitv
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split
bindsym $mod+f fullscreen toggle
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent

# Scratchpad
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# Resizing
mode "resize" {
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Afficher/masquer la barre (Waybar)
bindsym $mod+m bar mode toggle

# Utilities
bindsym --locked XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym --locked XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+
bindsym Print exec grim -g "$(slurp)" - | wl-copy  # Enhanced screenshot: select area and copy to clipboard

# Dev extras: Clipboard manager, notifications
exec wl-paste --type text --watch cliphist store  # Install cliphist for clipboard history
bindsym $mod+Shift+v exec cliphist list | wmenu | cliphist decode | wl-copy  # Paste from history

# Status Bar (switch to waybar for more modules: CPU, RAM, network, etc.)
# exec /usr/bin/waybar
# Lanceur d'applications
# Fallback to swaybar if waybar not installed
# Fallback to swaybar if waybar not installed
bar {
    # Afficher la barre uniquement sur le moniteur DP-8
    output DP-8,
    status_command waybar
    position top
}
# --- Règles pour les panneaux de configuration flottants ---

# Contrôle du son
for_window [app_id="pavucontrol"] floating enable, resize set width 500 px height 700 px

# Contrôle du réseau
for_window [app_id="nm-connection-editor"] floating enable, resize set width 650 px height 550 px

# Contrôle du Bluetooth
for_window [app_id="blueman-manager"] floating enable, resize set width 600 px height 500 px
# Controle TLP Battery
for_window [app_id="tlp-viewer"] floating enable, resize set width 1000 px height 1200 px
# Auto-start dev apps on workspaces (optional)
# assign [app_id="code"] workspace number 1:Code
# assign [app_id="wezterm"] workspace number 2:Term
# assign [class="brave-browser"] workspace number 3:Web

# Theme and gaps for better visuals
default_border pixel 2
gaps inner 5
gaps outer 5
smart_gaps on
smart_borders on
client.focused #4c7899 #285577 #ffffff #2e9ef4 #285577
client.unfocused #333333 #222222 #888888 #292d2e #222222

include /etc/sway/config.d/*
# Your custom exec
# exec /home/fard/dotfiles/scripts/sway/setup-dock.sh


</code>

wofi/.config/wofi/style.css:
<code>
/* --- Fenêtre principale --- */
window {
  margin: 10px;
  border: 2px solid #3498db;
  background: rgba(44, 62, 80, 0.95); /* Fond semi-transparent */
  border-radius: 18px;
  box-shadow: 0 6px 16px rgba(0, 0, 0, 0.5);
}

/* --- Champ de recherche --- */
#input {
  font-family: "Fira Code", monospace;
  font-size: 16px;
  margin: 10px;
  padding: 8px 12px;
  border: none;
  border-radius: 10px;
  color: #ecf0f1;
  background-color: #34495e;
  outline: none;
}

/* --- Conteneurs internes --- */
#inner-box, #outer-box {
  margin: 8px;
  border: none;
  background: transparent; /* On enlève tout fond derrière */
}

/* --- Liste déroulante --- */
#scroll {
  margin: 0;
  border: none;
  background: transparent;
}

/* --- Texte des entrées --- */
#text {
  margin: 6px;
  color: #ecf0f1;
  font-size: 15px;
  font-family: "Fira Code", monospace;
}

/* --- Entrées --- */
#entry {
  padding: 6px;
  border-radius: 10px;
  transition: background-color 0.2s ease;
  background: transparent; /* Supprime le fond noir derrière */
}

/* --- Entrée sélectionnée --- */
#entry:selected {
  background-color: #3498db;
  color: #ffffff;
  border-radius: 10px;
}

/* --- Entrée au focus --- */
#entry:focus {
  outline: none;
}
#entry {
  padding: 6px 10px;
  border-radius: 10px;
  background: transparent;
  display: flex;
  align-items: center; /* Centre l’icône avec le texte */
  gap: 8px; /* Espace entre icône et texte */
}

#entry:selected {
  background-color: #3498db;
  color: #ffffff;
  border-radius: 10px;
}

</code>

waybar/.config/waybar/config.old:
<code>
{
    // ==========================================
    // WAYBAR CONFIGURATION - FedoraSway
    // ==========================================
    
    // === BAR SETTINGS ===
    "layer": "top",
    "position": "top",
    "height": 24,
    "spacing": 3,
    "margin": 0,
    
    // === MODULES LAYOUT ===
    "modules-left": [
        "sway/workspaces",
        "sway/mode",
        "custom/separator",
        "cpu",
        "memory",
        "temperature"
    ],
    
    "modules-center": [
        "sway/window"
    ],
    
    "modules-right": [
        "idle_inhibitor",
        "pulseaudio",
        "custom/separator",
        "network",
        "bluetooth",
        "custom/separator",
        "backlight", 
        "battery",
        "custom/separator",
        "clock",
        "tray",
        "custom/power"
    ],

    // ==========================================
    // LEFT MODULES
    // ==========================================

    "sway/workspaces": {
        "disable-scroll": false,
        "all-outputs": true,
        "format": "{icon}",
        "persistent_workspaces": {
            "1": [],
            "2": [],
            "3": [],
            "4": [],
            "5": []
        },
        "format-icons": {
            "1": "1",        // Web
            "2": "2",        // Code  
            "3": "3",        // Files
            "4": "4",        // Chat
            "5": "5",        // Media
            "urgent": "!",
            "focused": "*",
            "default": "•"
        }
    },

    "sway/mode": {
        "format": "<span style=\"italic\">{}</span>",
        "max-length": 50
    },

    "cpu": {
        "interval": 3,
        "format": "CPU {usage}%",
        "tooltip": true,
        "tooltip-format": "CPU Usage: {usage}%\nLoad: {load}",
        "states": {
            "warning": 70,
            "critical": 85
        }
    },

    "memory": {
        "interval": 5,
        "format": "RAM {percentage}%",
        "tooltip": true,
        "tooltip-format": "RAM: {used:0.1f}G / {total:0.1f}G ({percentage}%)\nSwap: {swapUsed:0.1f}G / {swapTotal:0.1f}G",
        "states": {
            "warning": 75,
            "critical": 90
        }
    },

    "temperature": {
        "interval": 5,
        "thermal-zone": 0,
        "critical-threshold": 75,
        "format": "TEMP {temperatureC}°C",
        "tooltip": true,
        "tooltip-format": "Temperature: {temperatureC}°C"
    },

    // ==========================================
    // CENTER MODULES
    // ==========================================

    "sway/window": {
        "format": "{}",
        "max-length": 80,
        "tooltip": true,
        "rewrite": {
            "(.*) — Mozilla Firefox": " $1",
            "(.*) - Visual Studio Code": " $1", 
            "(.*) - vim": " $1",
            "(.*) - zsh": " $1",
            "(.*)": " $1"
        }
    },

    // ==========================================
    // RIGHT MODULES
    // ==========================================

    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "",
            "deactivated": ""
        },
        "tooltip": true,
        "tooltip-format-activated": "Idle inhibitor: Enabled",
        "tooltip-format-deactivated": "Idle inhibitor: Disabled"
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-bluetooth": "{icon} {volume}%",
        "format-bluetooth-muted": " {icon}",
        "format-muted": "",
        "format-icons": {
            "headphone": "",
            "hands-free": "",
            "headset": "",
            "phone": "",
            "portable": "",
            "car": "",
            "default": ["", "", ""]
        },
        "scroll-step": 5,
        "on-click": "pavucontrol",
        "on-click-right": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
        "tooltip": true,
        "tooltip-format": "Audio: {desc}\nVolume: {volume}%"
    },

    "network": {
        "interval": 3,
        "format-wifi": " {signalStrength}%",
        "format-ethernet": "",
        "format-linked": " No IP",
        "format-disconnected": "󰤭",
        "format-alt": "{ifname}: {ipaddr}/{cidr}",
        "tooltip": true,
        "tooltip-format-wifi": "Network: {essid}\nSignal: {signalStrength}%\nIP: {ipaddr}/{cidr}\nGateway: {gwaddr}",
        "tooltip-format-ethernet": "Ethernet: {ifname}\nIP: {ipaddr}/{cidr}",
        "tooltip-format-disconnected": "Network: Disconnected",
        "on-click-right": "nm-connection-editor"
    },

    "bluetooth": {
        "format": "BT",
        "format-disabled": "BT-",
        "format-off": "BT-", 
        "format-connected": "BT+",
        "tooltip": true,
        "tooltip-format": "Bluetooth: {status}",
        "tooltip-format-connected": "Bluetooth: Connected to {device_alias}",
        "on-click": "blueman-manager"
    },

    "backlight": {
        "format": "{icon} {percent}%",
        "format-icons": ["", "", "", "", "", "", "", "", ""],
        "tooltip": true,
        "tooltip-format": "Brightness: {percent}%",
        "on-scroll-up": "brightnessctl set 5%+",
        "on-scroll-down": "brightnessctl set 5%-"
    },

    "battery": {
        "interval": 10,
        "states": {
            "good": 80,
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "format-alt": "{icon} {time}",
        "format-icons": ["", "", "", "", ""],
        "tooltip": true,
        "tooltip-format": "Battery: {capacity}%\nTime: {timeTo}\nHealth: {health}%\nCycles: {cycles}"
    },

    "clock": {
        "interval": 60,
        "format": "{:%H:%M}",
        "format-alt": "{:%a %d %b}",
        "tooltip": true,
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "on-click": "gnome-calendar"
    },

    "tray": {
        "icon-size": 16,
        "spacing": 4,
        "show-passive-items": true
    },

    // ==========================================
    // CUSTOM MODULES
    // ==========================================

    "custom/separator": {
        "format": "│",
        "interval": "once",
        "tooltip": false
    },

    "custom/power": {
        "format": "PWR",
        "tooltip": true,
        "tooltip-format": "Power Menu",
        "on-click": "wlogout"
    }
}

</code>

waybar/.config/waybar/style.css:
<code>
/* Style CSS pour Waybar - Clean & Minimal avec icônes fixes */

* {
    font-family: "Fira Code", "FontAwesome", "Font Awesome 5 Free", monospace;
    font-size: 16px;
    font-weight: 500;
}

window#waybar {
    background: rgba(30, 30, 46, 0.4);
    border-bottom: 2px solid #3E4D5C;
    color: #cdd6f4;
}

button {
    border: none;
    background: transparent;
    margin: 2px;
    padding: 0 6px;
}

button:hover {
    background: rgba(137, 180, 250, 0.1);
}

/* === WORKSPACES === */
/* #workspaces { */
    /* background: rgba(49, 50, 68, 0.3); */
    /* border-radius: 8px; */
    /* margin: 2px 4px; */
    /* padding: 2px 4px; */
    /* border: 1px solid rgba(137, 180, 250, 0.2); */
/* } */

/* #workspaces button { */
    /* padding: 3px 8px; */
    /* margin: 1px; */
    /* background: transparent; */
    /* color: #7f849c; */
    /* border-radius: 6px; */
    /* min-width: 15px; */
/* } */

#workspaces button:hover {
    color: #cdd6f4;
    background: rgba(137, 180, 250, 0.2);
}

#workspaces button.active {
    color: #1e1e2e;
    background: linear-gradient(45deg, #89b4fa, #b4befe);
}

#workspaces button.focused {
    background: linear-gradient(45deg, #463578, #673578);
    color: #1e1e2e;
    animation: blink 3s linear infinite alternate;
}

#workspaces button.urgent {
    background: #f38ba8;
    color: #1e1e2e;
    animation: blink 1s linear infinite alternate;
}

/* === WINDOW === */
#window {
    color: #cdd6f4;
    font-weight: 400;
    margin: 2px 8px;
    padding: 0 4px;
}

/* === MODULES - CLEAN STYLE === */
#cpu {
    color: #89b4fa;
    margin: 0 6px;
    padding: 0;
}

#cpu.warning { color: #fab387; }
#cpu.critical { color: #f38ba8; animation: pulse 1s infinite; }

#memory {
    color: #a6e3a1;
    margin: 0 6px;
    padding: 0;
}

#memory.warning { color: #fab387; }
#memory.critical { color: #f38ba8; animation: pulse 1s infinite; }

#temperature {
    color: #f9e2af;
    margin: 0 6px;
    padding: 0;
}

#temperature.critical { color: #f38ba8; animation: pulse 2s infinite; }

#pulseaudio {
    color: #89b4fa;
    margin: 0 6px;
    padding: 0;
}

#pulseaudio.muted { color: #6c7086; }

#network {
    color: #a6e3a1;
    margin: 0 6px;
    padding: 0;
}

#network.disconnected { color: #f38ba8; }

#bluetooth {
    color: #89b4fa;
    margin: 0 6px;
    padding: 0;
    font-size: 20px;
    
}

#bluetooth.disabled, #bluetooth.off { color: #6c7086; }
#bluetooth.connected { color: #a6e3a1; }

#backlight {
    color: #f9e2af;
    margin: 0 6px;
    padding: 0;
}

#battery {
    color: #a6e3a1;
    margin: 0 6px;
    font-size: 18px;
    padding: 0;
}

#battery.charging, #battery.plugged { color: #a6e3a1; }
#battery.warning:not(.charging) { color: #fab387; }
#battery.critical:not(.charging) { color: #f38ba8; animation: blink 0.5s infinite; }

#idle_inhibitor {
    color: #cdd6f4;
    margin: 0 6px;
    font-size: 20px;
    padding: 0;
}

#idle_inhibitor.activated { color: #f38ba8; }

/* === CLOCK === */
#clock {
    /* background: linear-gradient(45deg, rgba(137, 180, 250, 0.15), rgba(180, 190, 254, 0.15)); */
    border-radius: 8px;
    margin: 2px 4px;
    padding: 4px 10px;
    color: #FFFFFF;
    font-weight: 600;
    font-size: 24px;
}

#clock:hover {
    background: linear-gradient(45deg, rgba(137, 180, 250, 0.25), rgba(180, 190, 254, 0.25));
}

/* === TRAY === */
#tray {
    margin: 2px 4px;
    padding: 0 4px;
}

/* === POWER & SEPARATORS === */
#custom-power {
    color: #f38ba8;
    margin: 0 6px;
    padding: 0;
}

#custom-power:hover { color: #fab387; }

#custom-separator {
    color: #45475a;
    margin: 0 6px;
    padding: 0;
    font-size: 33px;
}

#custom-uptime {
    color: #45475a;
    margin: 0 6px;
    padding: 0;
}

#mode {
    background: linear-gradient(45deg, #f38ba8, #fab387);
    color: #1e1e2e;
    border-radius: 6px;
    margin: 2px 4px;
    padding: 3px 8px;
    font-weight: 600;
    animation: pulse 2s infinite;
}

/* === ANIMATIONS === */
@keyframes blink {
    0% { opacity: 1; }
    50% { opacity: 0.5; }
    100% { opacity: 1; }
}

@keyframes pulse {
    0% { opacity: 1; }
    50% { opacity: 0.7; }
    100% { opacity: 1; }
}
/* ---- STYLE DE L'INFOBULLE (TOOLTIP) ---- */
tooltip {
    background-color: #1E1E2E;         /* Un fond sombre, cohérent avec un thème dark. */
    border: 1px solid #3E4D5C;         /* Une bordure de couleur (ici, un bleu "Catppuccin Macchiato"). */
    border-radius: 4px;                /* Des bords arrondis pour adoucir la forme. */
    padding: 8px;                      /* Un peu d'espace interne pour que le texte ne soit pas collé au bord. */
}

tooltip label {
    color: #CDD6F4;                    /* Une couleur de texte claire et lisible sur fond sombre. */
    font-size: 17px;                   /* Une taille de police confortable pour la lecture. */
}

</code>

waybar/.config/waybar/config:
<code>
{
    // ==========================================
    // WAYBAR CONFIGURATION - RATIONALIZED
    // ==========================================
    
    // === BAR SETTINGS ===
    "layer": "top",
    "output" : ["DP-8", "eDP-1"],
    "position": "top",
    "height": 30,
    "spacing": 3,
    "margin": 0,
    
    // === MODULES LAYOUT ===
    "modules-left": [
        "sway/workspaces",
        "custom/separator",
        "sway/mode",
        "custom/uptime",
        "cpu",
        "memory",
        "temperature"
    ],
    
    "modules-center": [
        "clock"
    ],
    
    "modules-right": [
        "idle_inhibitor",
        "pulseaudio",
        "custom/separator",
        "network",
        "bluetooth",
        "custom/separator",
        "backlight", 
        "battery",
        "custom/separator",
        "tray",
        "custom/power"
    ],

    // ==========================================
    // LEFT MODULES
    // ==========================================

    "sway/workspaces": {
        "disable-scroll": false,
        "all-outputs": true,
        "format": "{name}", // ACTION : Affiche le nom directement. C'est plus propre.
        "format-icons": {
            "urgent": "!",
            "default": "•"
            // "focused" est géré par le CSS.
        }
    },

    "sway/mode": {
        "format": "<span style=\"italic\">{}</span>",
        "max-length": 50
    },

    "custom/uptime": {
        "format": "{}'",
        "exec": "awk '{printf \"%d\", $1/60}' /proc/uptime",
        "interval": 60
    },
    
    "cpu": {
        "interval": 3,
        "format": "{usage}%", // ACTION : Ajout d'icône.
        "tooltip": true,
        "on-click": "kitty -e btop" // ACTION : Rend le module interactif.
    },

    "memory": {
        "interval": 5,
        "format": "🗲{percentage}%", // ACTION : Ajout d'icône.
        "tooltip": true,
        "on-click": "kitty -e btop" // ACTION : Rend le module interactif.
    },

    "temperature": {
        "interval": 5,
        "thermal-zone": 0,
        "critical-threshold": 75,
        "format": "{temperatureC}°C" // ACTION : Ajout d'icône.
    },

    // ==========================================
    // CENTER MODULES
    // ==========================================

      "clock": {
          "format": "{0:%H:%M}",
          "tooltip-format": "{0:%A %d %B}\n  Semaine n°{0:%V}",
          "on-click": "gnome-calendar"
    },
    // ==========================================
    // RIGHT MODULES
    // ==========================================

    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "", // ACTION : Icônes claires
            "deactivated": "",
        }
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": " {volume}%",
        "format-icons": {
            "headphone": "",
            "hands-free": "",
            "analog-output-headphones": "",
            "analog-output" : "",
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol",
        "on-scroll-up": "pamixer --increase 5",
        "on-scroll-down": "pamixer --decrease 5"
    },

    "network": {
        "interval": 3,
        "format-wifi": " {signalStrength}%",
        "format-ethernet": "",
        "format-disconnected": "󰤭",
        "tooltip-format-wifi": "IP: {ipaddr}",
        "tooltip-format-ethernet": "Interface: {ifname}\n    IP: {ipaddr}",
        "on-click": "nm-connection-editor",
        "on-click-right": "kitty -e nmtui"
    },  

    "bluetooth": {
        "format": "", // ACTION : Icônes pour chaque état
        "format-disabled": "󰂲",
        "format-off": "󰂲",
        "format-connected": "",
        "on-click": "blueman-manager"
    },

    "backlight": {
        "format": "{icon} {percent}%",
        "format-icons": ["", "", "", "", "", "", "", "", ""] // ACTION : Icônes
    },

    "battery": {
        "states": { "warning": 30, "critical": 15 },
        "format": "{icon} {capacity}%",
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "format-icons": ["", "", "", "", ""],
    "on-click": "kitty -o confirm_os_window_close=0 --class tlp-viewer ~/dotfiles/sway/scripts/tlp-menu.sh"        },

    "tray": {
        "icon-size": 20,
        "spacing": 4
    },

    "custom/separator": { "format": "⫶" },

    "custom/power": {
        "format": "", // ACTION : Icône
        "on-click": "wlogout"
    }
}

</code>

gtk-3.0/.config/gtk-3.0/bookmarks:
<code>
file:///home/fard/Downloads Downloads 📥
file:///home/fard/Documents Documents 📚
file:///home/fard/Documents/10_SYNC/10.2_VAULTS/10.2.1_SUMAC_CLOUD/20.DOMAINS/D01_WHEELHOUSE Wheelhouse 🧭
file:///home/fard/Documents/10_SYNC/10.2_VAULTS/10.2.1_SUMAC_CLOUD/20.DOMAINS/D02_AI_TOOLS AI Tools ✴️
file:///mnt/sumac/dev/warehouse/playground Playground 📑
file:/// Root 🛠️
file:///home/fard/Documents/10_SYNC/10.2_VAULTS/10.2.1_SUMAC_CLOUD Obsidian 💎
file:///mnt/sumac Sumac 🧪
file:///mnt/sumac/dev/warehouse/projects/professional/korrigan korrigan

</code>

gtk-3.0/.config/gtk-3.0/settings.ini:
<code>

[Settings]
gtk-theme-name=Adwaita-dark
gtk-application-prefer-dark-theme=1
gtk-icon-theme-name=Papirus-Dark

</code>

scripts/setup-dock.sh:
<code>

#!/bin/bash

# --- LA SOLUTION À LA CONDITION DE COURSE ---
# On attend 3 secondes pour être certain que tous les 
# moniteurs connectés au dock sont bien initialisés par le système.
sleep 3

swaymsg "output \"AOC 1601W MMEM1JA000545\" enable pos 0 856 res 1920x1080 transform 270; output \"Samsung Electric Company Odyssey G5 HK7X400599\" enable pos 1080 856 res 2560x1440@119.998Hz; output \"Samsung Electric Company LS32CG51x H9JX201723\" enable pos 3640 0 res 2560x1440 transform 90; output eDP-1 disable;"
# swaymsg "output \"AOC 1601W MMEM1JA000545\" enable pos 0 856 res 1920x1080 transform 270; output \"Samsung Electric Company Odyssey G5 HK7X400599\" enable pos 1080 856 res 2560x1440@119.998Hz; output \"Samsung Electric Company LS32CG51x H9JX201723\" enable pos 3640 0 res 2560x1440 transform 90; output eDP-1 disable; workspace 1 output \"Samsung Electric Company Odyssey G5 HK7X400599\";"
# swaymsg "output \"AOC 1601W MMEM1JA000545\" enable pos 0 856 res 1920x1080 transform 270; output \"Samsung Electric Company Odyssey G5 HK7X400599\" enable pos 1080 856 res 2560x1440@119.998Hz; output \"Samsung Electric Company LS32CG51x H9JX201723\" enable pos 3640 0 res 2560x1440 transform 90; output eDP-1 disable; workspace 1 output \"Samsung Electric Company Odyssey G5 HK7X400599\"; output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill"

</code>

scripts/tlp-menu.sh:
<code>
#!/bin/bash

# Vérifier si 'gum' est installé
if ! command -v gum &> /dev/null; then
    kitty --hold echo "Le paquet 'gum' est nécessaire. Voir les instructions d'installation."
    exit 1
fi

# Boucle principale du script
while true; do
    # On nettoie l'écran et on affiche le statut en premier
    clear
    
    # MODIFICATION ICI : On retire le cadre et la couleur pour un affichage simple.
    echo "--- État Actuel de la Batterie (TLP) ---"
    sudo tlp-stat -s -b
    echo "------------------------------------------"
    echo

    # gum choose crée un menu sélectionnable avec les flèches
    choice=$(gum choose "Forcer charge complète" "Recalibrer batterie" "Définir les seuils" "Quitter" --height=4)

    # Si l'utilisateur appuie sur Echap, gum retourne une chaîne vide
    if [ -z "$choice" ]; then
        clear
        break
    fi

    case "$choice" in
        "Forcer charge complète")
            gum confirm "Lancer une charge complète pour BAT0 ?" && sudo tlp fullcharge BAT0
            ;;
        "Recalibrer batterie")
            gum confirm "Lancer le recalibrage pour BAT0 ?" && sudo tlp recalibrate BAT0
            ;;
        "Définir les seuils")
            start_charge=$(gum input --placeholder "Seuil de DÉBUT (ex: 70)" --prompt "│ ")
            if [ $? -ne 0 ]; then continue; fi
            
            stop_charge=$(gum input --placeholder "Seuil de FIN (ex: 80)" --prompt "│ ")
            if [ $? -ne 0 ]; then continue; fi

            if [ -n "$start_charge" ] && [ -n "$stop_charge" ]; then
                gum confirm "Régler les seuils à $start_charge% - $stop_charge% ?" && sudo tlp setcharge "$start_charge" "$stop_charge" BAT0
            fi
            ;;
        "Quitter")
            clear
            break
            ;;
    esac
    sleep 1
done

</code>

kanshi/.config/kanshi/config:
<code>
# Profil "Maison" : Syntaxe finale compacte et rigoureuse.
profile maison {

    output "eDP-1" disable

    output "AOC 1601W MMEM1JA000545"  enable mode 1920x1080 position 0,856 transform 90 

    output "Samsung Electric Company Odyssey G5 HK7X400599"  enable mode 2560x1440@119.998Hz position 1080,856 

    output "Samsung Electric Company LS32CG51x H9JX201723" enable mode 2560x1440 position 3640,0 transform 270 
}

# Profil "Nomade"
profile nomade {
    output "eDP-1"  enable mode 1920x1200 position 0,0 
}

</code>



Fait un script pour installer TOUTES les dependances necessaire a l execution de SWAY et des dependances !